import React from 'react'

const LoadingProgressBar = () => {
  return (
    <div>LoadingProgressBar</div>
  )
}

export default LoadingProgressBar

// todo : add loading progress bar in detect page and detect grid table